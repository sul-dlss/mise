<h1>New Resource</h1>

<%= form_with(model: @resource, url: project_resources_path(@resource)) do |form| %>
  <%= render 'form', resource: @resource, form: form %>
<% end %>

<%= link_to 'Back', project_resources_path(@resource.project) %>
